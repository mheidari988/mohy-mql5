# Artifacts

- `runs/`: generated run artifacts during local debug/analysis (not committed by default).
- `samples/`: optional curated artifacts committed for reproducible demonstrations.

If you want to keep one run permanently, copy it from `runs/` to `samples/` and reference it in docs.
