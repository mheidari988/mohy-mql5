# Examples

Curated explanation artifacts and scripted synthetic scenarios.

- Example artifacts should focus on active kernel layers only: `elements`, `legs`, `swings3`, `potential_impulses`, `potential_corrections`, `potential_continuation_signals`, and `trade_setup_plans`.
- Continuation examples should use structural confirmation anchors, while executable entry/stop/target examples belong under `trade_setup_plans`.

Keep example artifacts small and deterministic so visual discussions remain stable across revisions.
